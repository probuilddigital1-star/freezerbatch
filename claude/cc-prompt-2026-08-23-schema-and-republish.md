# CC prompt, 2026-08-23 (fourth): recipe schema images, then the republish

Standalone prompt. Start a fresh Claude Code session and do not rely on prior context.

Repo: `C:\Users\zckpe\Documents\claude-projects\freezer-batch-cocktails`

## Where things stand

Shipped and verified live earlier today: the Labor Day guide page, the inline signup
after the calculator with `placement` instrumentation, the photo pipeline commit, and
per-page og images. All 18 recipe pages now serve a real 1200x630 JPEG at
`/images/cocktails/<slug>-og.jpg`, 8 photographs and 10 typographic cards generated by
`scripts/generate-og-images.mjs`. `og-default.svg` is gone.

Two of the three follow-ups you raised are tasks 1 and 2 below. Do not re-litigate the
gitignore or the card generator; both were right.

## Task 1: give the Recipe structured data an image

Recipe pages emit Recipe structured data with no `image` property. Google's recipe rich
results want one, and search is currently the only channel producing visitors to this
site, so this is the highest-value SEO change available.

Add `image` to the Recipe JSON-LD on `src/pages/cocktails/[slug].astro`, pointing at the
same absolute `/images/cocktails/<slug>-og.jpg` the og tags already resolve to. One
source of truth for the path; derive it the same way `Layout.astro` does rather than
rebuilding the string.

While you are in there, report (do not fix) whether the Recipe schema is missing any
other property Google treats as recommended for rich results, particularly `recipeYield`,
`totalTime`, `recipeIngredient` and `recipeInstructions`. If any are absent I want to
know before deciding whether to add them.

**Known limitation, deliberately accepted for now.** Google prefers multiple aspect
ratios (16:9, 4:3, 1:1) and the og render is roughly 1.9:1. The 4:5 card renders that
would supply the other ratios are still gitignored until all 18 photos are shot. Note it
in your report; the upgrade belongs to the publish commit, not this task.

## Task 2: og:type

Every page emits `og:type: website`, including the four blog guides and the new Labor Day
guide. `article` is correct for those. Fix it for anything under `/blog/`, leave the rest.

## Task 3: clean up the avatar exploration

`social/avatars/` holds 15 Instagram profile-picture candidates, about 2.7MB, and is
untracked. Fourteen of them are rejected exploration and do not belong in a public repo.

Keep `fbc-avatar-FINAL-old-fashioned.jpg`, which is the chosen one and now the live
Instagram profile picture, and `fbc-mint-julep-band.jpg` as the one alternate worth
holding. Move the other thirteen to `_to_delete/`, which is already ignored, the same way
you handled the reject frames. Then track the two that remain, consistent with the
earlier call on `social/labor-day-2026/`.

## Gates

`astro check`, `astro build`, unit, e2e, the analytics check, `check:hosts`.

Live verification, asserting content rather than status:

- the margarita page's Recipe JSON-LD contains an absolute `image` ending
  `margarita-og.jpg`, and it parses as valid JSON-LD
- the same for `paper-plane`, a typographic card rather than a photograph
- `/blog/batch-ahead-for-labor-day/` emits `og:type: article`
- `/cocktails/margarita/` still emits `og:type: website`
- validate at least one recipe page against Google's Rich Results Test if you can reach
  it; if not, say so rather than implying you did

Verify against `freezerbatchcocktails.com`, never a preview or `*.pages.dev` URL.

---

## STOP HERE. Report on tasks 1 to 3 and wait for approval.

Everything below mutates the live email pipeline. API PUTs go live immediately on this
n8n instance and there is no draft step.

---

## Task 4: the Cold Open republish, now with images

`claude/cc-prompt-2026-08-23-send.md` is on disk and gitignored. It holds the full
procedure and the `POSTAL_ADDRESS` value. **Read it from there. Do not copy that address
into any tracked file, commit message, or report.** It is a home address and this repo is
public. You made that call earlier today and it was the right one.

One thing in that prompt has changed. It said the welcome email's image row was blocked
because `/images/cocktails/negroni-og.jpg` returned the 404 page. It now returns a real
`image/jpeg`. So:

- Restore the welcome email's image row pointing at the deployed negroni og URL. The
  08-01 removal took the whole row out, so this is an edit rather than a toggle.
- Issue 01's hero image is likewise unblocked, using the margarita og URL.

Follow `n8n/republish-2026-08/APPLY.md` exactly: build onto a fresh live export, show the
diff and stop for approval before the PUT, never PUT the stripped review JSON, abort if
any node beyond the two expected build nodes differs. After the PUT, run a live signup
test and confirm the welcome email arrives with the label sheet, the footer address, and
the image row rendering rather than showing a broken image.

## Report

For every task: what changed, what the gates said, what live verification returned, and
anything you could not make safe. For task 1, include the list of missing recommended
Recipe properties. For task 4, include the exact diff you PUT and what the live signup
test returned, with the postal address redacted from the report itself.
