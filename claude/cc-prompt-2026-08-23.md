# CC prompt, 2026-08-23: ship the Labor Day guide, fix signup placement

Standalone prompt. Start a fresh Claude Code session; do not rely on prior context.

Repo: `C:\Users\zckpe\Documents\claude-projects\freezer-batch-cocktails` (Astro static
site, Cloudflare Pages, PostHog analytics).

---

## Why this work exists (measured, 2026-08-23)

Three findings from PostHog, all real numbers, not guesses:

1. **The site gets 1 to 4 real visitors a day.** ~190 pageviews over 30 days. Every
   conversion fix shipped in the last week is working correctly for almost nobody.
2. **Search is the only channel producing visitors.** Google, Bing, DuckDuckGo, Yahoo
   and Ecosia all send traffic. `/cocktails/paper-plane/` drew 10 of its 11 pageviews
   from search engines. Recipe pages are indexed and ranking.
3. **The recipe-page Cold Open card has never converted, once, in the site's history.**
   All six newsletter signups ever recorded came from the homepage. The card is not
   broken; it is buried. In `src/pages/cocktails/[slug].astro` it renders at line ~295,
   after the calculator, the serving notes, the pro tips, the affiliate block and the
   recommended-tools block.

So: more search-facing content, and a signup ask placed where intent actually peaks.

---

## Task 1: ship the new guide page (files already on disk)

Two files were written into the working tree by Cowork and are **not yet committed**:

- `src/pages/blog/batch-ahead-for-labor-day.astro` (new)
- `src/pages/blog/index.astro` (modified: new post prepended to the `posts` array)

The page follows the conventions of `src/pages/blog/dilution-guide.astro` (same Layout,
breadcrumb, `article-eyebrow` / `article-headline` / `article-deck`, `editorial` body,
`editorial-recap` aside, closing CTA box). It targets make-ahead and Labor Day cocktail
search intent and links to `/cocktails/margarita/`.

Do:

1. Read both files and confirm they match house conventions. Fix any class or import
   mismatch you find; report anything you change.
2. Confirm the page is picked up by `src/pages/sitemap-index.xml.ts`. If the sitemap
   enumerates blog routes explicitly rather than by glob, add this route.
3. Commit both files together with a clear message.

## Task 2: put the signup where the intent is

In `src/pages/cocktails/[slug].astro`, add a **second** `EmailSignup` instance directly
after the calculator section (currently lines ~199 to 201), before "How to serve".

Rationale: a visitor who has just sized a batch to their bottle is the single most
qualified moment on the page, and the offer (a printable bottle-label sheet) is exactly
what that person needs next. Keep the existing bottom-of-page card as well; it costs
nothing and catches readers who scroll past.

`src/components/EmailSignup.astro` already accepts `headline`, `promise`, `ctaLabel`,
`reassurance` and `programName` as props, so no change to its load-bearing internals
(Turnstile gating, honeypot, requestId reuse, analytics) is needed or wanted.

Suggested copy for the new instance, adjust if it fights the layout:

- headline: `Label the bottle before it goes in.`
- promise: `Free printable label sheet, plus one make-ahead recipe and a hosting timeline each month.`
- ctaLabel: `Get the free label sheet`

**The real risk here is duplicate-instance safety.** Two `<email-signup-form>` custom
elements on one page. Before shipping, verify all of the following and report on each:

- Any `id` attribute inside the component is now duplicated in the DOM. If so, make ids
  instance-scoped.
- Turnstile renders one widget per instance and neither blocks the other.
- Submitting one form does not mutate or disable the other.
- The honeypot and requestId logic still behave per-instance.

If any of these cannot be made safe without touching the shared submit logic, stop and
report rather than refactoring that logic on your own.

## Task 3: instrument placement

`newsletter_optin` currently carries `page_type` derived from the pathname, so both
recipe-page instances would report identically and we could not tell which one earned
the signup.

Add a `placement` property to the `newsletter_optin` event with a stable value per
instance (for example `inline-post-calculator`, `page-bottom`, `homepage-footer`).
Source it from the component's existing `data-variant` pattern or a new optional prop.
Update `src/lib/analytics.ts` and its unit tests accordingly.

---

## Gates: all must pass before you push

- `astro check`
- `astro build`
- unit tests
- e2e tests
- the analytics check
- `check:hosts`
- **Live verification after deploy**, asserting behavior and not HTTP 200:
  - `/blog/batch-ahead-for-labor-day/` returns the guide, headline and CTA present
  - `/blog/` lists the new guide first
  - the sitemap contains the new route
  - on a recipe page, both signup forms render and **each can be submitted
    independently** with the other still functional

Do not verify against a preview or `*.pages.dev` URL. Those builds carry the production
PostHog key and pollute real analytics. Verify against `freezerbatchcocktails.com`.

## Report

Report plainly what you changed, what the gates said, and anything you could not make
safe. If Task 2's duplicate-instance check turned up a problem you worked around rather
than solved, say so in those words.
